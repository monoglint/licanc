# driver
This subfolder is responsible for organizing compiler calls and flags into the pipeline and returning a final bytecode file with metadata.

To note: base ID types for intern pools are stored in driver_base to prevent circular dependencies.